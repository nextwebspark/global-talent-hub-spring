# Stop Lombok config lookup at the project root.
config.stopBubbling = true

# Copy Spring's @Qualifier and @Value onto constructor parameters Lombok generates
# (@RequiredArgsConstructor), so qualified bean injection works without an explicit ctor.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Value
